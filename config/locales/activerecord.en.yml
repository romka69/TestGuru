ru:
  activerecord:
    models:
      test: "Test"

    attributes:
      test:
        title: "Title"
        category: "Category"

      badge:
        rule: "Rule"
        file: "Link on image"
        param: "Param"

    errors:
      messages:
        blank: "can't be blank"
        required: "must exist"
