ru:
  activerecord:
    models:
      test: "Test"

    attributes:
      test:
        title: "Title"
        category: "Category"

    errors:
      messages:
        blank: "can't be blank"
        required: "must exist"
